import { config as configBase } from '@tamagui/config/v3';
import { createTamagui, TamaguiInternalConfig } from 'tamagui';

const config: TamaguiInternalConfig = createTamagui(configBase);

export default config;
